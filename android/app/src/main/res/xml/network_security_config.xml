<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Development only: arbitrary LAN devHost values cannot be expressed as an Android domain allowlist. Keep production traffic on HTTPS and tighten this before release. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
